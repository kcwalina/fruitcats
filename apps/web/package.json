{
  "name": "@fruitcats/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p ../../tsconfig.json --noEmit && vite build",
    "build:playtest": "tsc -p ../../tsconfig.json --noEmit && vite build --mode playtest",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fruitcats/engine": "*",
    "@fruitcats/match": "*",
    "@fruitcats/store": "*"
  },
  "devDependencies": {
    "marked": "^18.0.14"
  }
}
